/*
Função comum.

function somar(x, y) {
	return x + y;
}

*/

/*
Função na variável.

let somar = function(x, y) {
	return x + y;
};

*/

/*
Arrow function comum.

let somar = (x, y) => {
	return x + y;
};
*/

let somar = (x, y) => x + y;
console.log(somar(10, 5));
